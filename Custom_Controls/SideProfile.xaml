<UserControl x:Class="LynxUI_Main.Custom_Controls.SideProfile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LynxUI_Main.Custom_Controls"
             xmlns:converters="clr-namespace:LynxUI_Main.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InvertedBoolToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" />
        <converters:SafeImagePathConverter x:Key="SafeImagePathConverter" />
        <converters:GroupAvatarPositionConverter x:Key="GroupAvatarPositionConverter" />
    </UserControl.Resources>

    <Grid>
        <StackPanel Margin="20" HorizontalAlignment="Center">
            <!-- Avatar Group (If Group Chat) -->
            <Grid Width="80" Height="80" Visibility="{Binding IsGroup, Converter={StaticResource BoolToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding AvatarSlots}" AlternationCount="10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas Width="80" Height="80" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Image Width="48" Height="48"
                                   Source="{Binding Converter={StaticResource SafeImagePathConverter}}"
                                   Stretch="UniformToFill" ClipToBounds="True">
                                <Image.Clip>
                                    <EllipseGeometry Center="24,24" RadiusX="24" RadiusY="24" />
                                </Image.Clip>
                            </Image>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource GroupAvatarPositionConverter}" ConverterParameter="Left">
                                        <Binding RelativeSource="{RelativeSource Self}" Path="(ItemsControl.AlternationIndex)" />
                                        <Binding Path="DataContext.AvatarSlots.Count" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Canvas.Top">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource GroupAvatarPositionConverter}" ConverterParameter="Top">
                                        <Binding RelativeSource="{RelativeSource Self}" Path="(ItemsControl.AlternationIndex)" />
                                        <Binding Path="DataContext.AvatarSlots.Count" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </Grid>

            <!-- Avatar cá nhân (nếu không phải group) -->
            <Image Width="80" Height="80"
                   Source="{Binding AvatarImageSource, Converter={StaticResource SafeImagePathConverter}}"
                   Visibility="{Binding IsGroup, Converter={StaticResource InvertedBoolToVisibilityConverter}}"
                   Stretch="UniformToFill" ClipToBounds="True" Margin="0,10">
                <Image.Clip>
                    <EllipseGeometry Center="40,40" RadiusX="40" RadiusY="40" />
                </Image.Clip>
            </Image>

            <!-- Display Name -->
            <TextBlock Text="{Binding DisplayName}"
                       FontSize="20" FontWeight="Bold" TextAlignment="Center"
                       Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
