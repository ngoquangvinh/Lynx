<?xml version="1.0" encoding="UTF-8"?>
<UserControl x:Class="Login1.Controls.TitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="20" >
    <Border Background="#22000000" Height="20" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#667eea" Offset="0.0"/>
                    <GradientStop Color="#764ba2" Offset="1.0"/>
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <Image Source="/img1/Lynx_V2.png" Width="20" Height="20" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock Text="Lynx" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="1" x:Name="MinimizeButton" Content="–" Width="20" Click="Minimize_Click" />
            <Button Grid.Column="2" x:Name="MaximizeButton" Content="▢" Width="20" Click="Maximize_Click" />
            <Button Grid.Column="3" x:Name="CloseButton" Content="X" Width="20"  Click="Close_Click" />
        </Grid>
    </Border>
</UserControl>
